extends layout

block content
  h1= title
  p Welcome to #{title}
  if messages.length
    ul
      each message in messages
        li
          p #{message.user} (#{message.added}): #{message.text}
